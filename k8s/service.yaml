apiVersion: v1
kind: Service
metadata:
  name: vainterior-svc
  namespace: vainterior
spec:
  type: NodePort
  selector:
    app: vainterior
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
